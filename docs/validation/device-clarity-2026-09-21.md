# 2.2.2 设备直连与可读性验证

代码提交：389e4c4。版本 2.2.2+19。所有编译和打包均在本机或本地 Windows 11 虚拟机完成；服务器只接收校验后的网站与安装包成品。

## 已验证

- Flutter 完整测试 149 通过，1 项 Windows 专用测试在 macOS 跳过；静态分析无 error/warning，保留仓库原有风格提示。
- 同账号卡片直达远控且不复用旧密码；取消、退出账号、切换服务器后的迟到会话被断开；实际失败反馈与重试；跨服务器记录拦截；旧来源确认；IPv4/端口收藏保持直连。
- 账号归属仅从当前服务器账号快照产生。未隔离服务器的旧缓存密码不在新卡片连接入口自动读取或发送。
- 浅色/深色主题 ChoiceChip 和 FilterChip 选中、未选中文字实际渲染对比度均 >=4.5；390px 宽、1.6 倍字号无溢出。
- 真实公网 API：本轮创建的临时主机，同账号空密码成功获得授权；其他账号错误密码被拒。临时主机及两个测试账号已清理。此项验证协议，不代表真实远控画面或物理 WOL。
- Windows：本地 VM x64 构建、主程序启动、安装程序生成和临时安装检查通过。发布副本的 Dart 源码与最终提交逐文件一致。VM 恢复原暂停状态。
- Android：原发布密钥签名验证通过，包名 com.qsw.rdesk，版本 2.2.2(19)。没有连接的安卓实机，未进行本轮安装交接。
- Mac：arm64/x86_64 主程序与 Flutter 代码架构匹配；Developer ID、Hardened Runtime 验证通过；两个应用和 DMG 均获得 Apple 公证 Accepted 并 staple/validate。保留原已安装的本机应用，避免覆盖另一项进行中的捕获改动。
- iPhone：本地 Release 构建通过，2.2.2(19) 已保留。安装因设备锁屏、无法挂载开发者镜像被系统拒绝（CoreDevice 12040 / 10003）；已请求用户解锁，尚未安装本轮版本。未提交 App Store。

## 发布与边界

官网版本目录 `/opt/rdesk-website/releases/20260921-2.2.2`；切换前备份链接 `/opt/rdesk-website/backups/20260921-device-clarity-2.2.2/previous-current.txt`。原子切换 current，保留旧版安装包 URL；不改域名根入口、账号服务或转发协议。

线上版本清单与本地逐字段一致，浏览器刷新已显示 2.2.2；公开文件完整校验及生产更新服务检查记录在本轮 evidence 目录。

RDesk 当前开机助手仍为安卓端。远程开机原理不限定 Android，但尚未实现路由器助手，未证明小米 AX3000 固件兼容；不能复用 UU 私有助手。助手与电脑只需同一家庭网络，不需要紧挨电脑。Windows 仍不支持被远控；实体开机和隔夜稳定性未进行本轮验收。

本地证据目录：`.release-work/20260921/device-clarity/`（不提交二进制、密钥、设备隐私）。
