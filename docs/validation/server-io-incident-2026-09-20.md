# 2026-09-20 服务器 I/O 拥塞调查与优化建议

## 结论与边界

直接故障已确认是持续磁盘读 I/O 饱和，造成系统级 I/O 等待和大量进程阻塞；不是容量耗尽，也未查到本次 OOM 杀进程记录。最可能的触发因素是本轮在内存余量很低的生产机上执行 Rust 构建，引起内存回收和文件页反复读入。没有故障期间的逐进程 I/O 历史，不能证明所有读盘量均来自 rustc，也不能声称重启修复了根因。

生产实例为 ecs.e-c1m1.large，2 vCPU / 2 GiB。操作系统可见内存 1891 MiB，运行 8 个 Docker 容器（包括 MySQL 和多个 Node/Next 服务）及 RDesk。服务器端构建使用 CARGO_BUILD_JOBS=1，但没有内存或读写限额；这种执行方式不足以保护生产。

## 时间线与证据

所有时间为北京时间，数据取自重启后仍保留的 `/var/log/sa/sa20` 与上一启动周期 journal。

| 指标 | 11:40:04 正常基线 | 12:00:36 故障期间 | 重启后验证 |
|---|---:|---:|---:|
| CPU I/O 等待 | 0.03% | 91.51% | 5 秒采样平均 0% |
| 磁盘读取 | 77.11 KiB/s | 108073.60 KiB/s（约 105.5 MiB/s） | 短采样接近 0 |
| 磁盘忙碌率 | 0.15% | 98.68% | 短采样接近 0 |
| 平均 I/O 等待时间 | 2.42 ms | 123.57 ms | 无拥塞 |
| 阻塞进程 | 0 | 52 | 当前 vmstat 0 |
| 一分钟负载 | 0.03 | 45.39 | 启动后回落至 0.76 |
| 可用内存 | 约 389 MiB | 约 144 MiB | 约 680–726 MiB |

- 编译日志最后更新时间 11:47:41，停在编译 syn；没有构建成功记录。原 target/release/rdesk-server 仍是 8 月 12 日旧文件，不能当作本轮制品。
- 11:46 起 Docker 健康检查开始超时；11:49 SSH 会话创建超时；13:16 systemd-logind 连停止操作都超时。
- 12:00–13:20 读盘持续约 105.5 MiB/s，I/O 等待 91.5%–93.7%，磁盘忙碌率约 98.6%–98.8%。
- 同期频繁换入/换出，并有大量文件页回收；swap 用量只有约 260 MiB，不能简化为“2 GiB swap 被用满”。
- 用户提供的短信明确是 Instance:StoragePerformanceReachLimit；官方定义为实例磁盘带宽或 IOPS 达上限，不是容量告警。
- 用户发起重启后，13:24:22 开始新启动周期；SSH、RDesk 服务、Docker 和公网 /health 恢复。/health 返回 200，约 0.13 秒，根站返回 200，约 0.10 秒。
- 重启后没有 cargo/rustc 残留。RDesk 服务内存约 5 MiB，单凭本次证据不支持将运行时 RDesk 判定为主要内存占用者。

## 容量与配置发现

- 根分区 40 GiB，已用 32 GiB，可用 5.9 GiB，使用率 85%；inode 使用率 19%。容量需要保留余量，但不是此次告警的含义。
- Docker 镜像约 6.5 GB，其中约 3.1 GB 被报告为可回收。该数值不是删除授权；先核对回滚保留需求，再定向清理。业务卷不在清理建议中。
- MySQL 当前约 417 MiB，qingsong-notes 约 190 MiB，siteharbor 约 102 MiB，其他服务和系统还需额外内存。
- `/etc/sysctl.d/99-siteharbor-lowmem.conf` 设置 swappiness=10，但 `/etc/sysctl.d/99-sysctl.conf` 链接到 `/etc/sysctl.conf`，后者值为 0，实际运行值为 0。配置冲突确认存在；尚未在生产修改。
- 目前容器未设置独立内存限额。限额应按业务峰值评估后设置，避免直接套用小限额导致服务被杀。
- 已有 2 GiB swap；dnf-makecache.timer 为 disabled，无需重复增加 swap 或再次关闭该定时器。

## 优化顺序

1. **先分离构建与运行**：CI / 独立构建机生成与 Anolis 8 兼容的成品；生产只做上传、替换、校验与必要的服务重启。不要在这台生产机进行压力复现。已更新部署说明。
2. **补内存余量**：当前多个服务共用 2 GiB 余量偏紧。若持续保留全部服务，可评估 2 核 4 GiB 起步、8 GiB 更宽裕；也可拆分数据库或非必要服务。本次 CPU 计算不是瓶颈，不应只加 CPU 核数。
3. **统一 swap 策略**：消除 0 与 10 的覆盖冲突，选择一个明确的配置来源；在维护窗口试用 10 并对比工作负载。它不是根治，也不能替代构建隔离和内存容量。不要 swapoff，也不要把 swap 无限增大。
4. **限额和观测**：按实际峰值为服务制定内存预算；构建环境独立限 CPU / 内存 / I/O。将主机采样缩短至约 1 分钟，关注 MemAvailable、iowait、await、实例带宽和 IOPS。建议起始告警：MemAvailable 持续低于 300 MiB、iowait 持续超过 20%、文件系统使用率超过 85%，再根据基线调整。
5. **容量整理为次要工作**：审查未使用镜像、历史构建目录和日志保留，目标保留至少约 20% 空间。不要执行无差别 docker prune 或删除业务卷。

本轮仅进行了取证、恢复状态验证、官网故障提示撤除和部署说明更新；没有购买扩容、清理业务数据、改数据库参数或重新在生产构建。远程开机配套服务仍未部署。

## 参考

- [阿里云性能风险事件定义](https://help.aliyun.com/zh/ecs/user-guide/performance-risk-event-notification)
- [Linux swappiness 含义](https://kernel.org/doc/html/latest/admin-guide/sysctl/vm.html#swappiness)
